---
phase: 02-design-system
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - components.json
  - src/lib/utils.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/lib/format/index.ts
autonomous: true
requirements: [FOUND-02, FOUND-03, DS-01, DS-03]
must_haves:
  truths:
    - "Every prototype `:root` token from styles.css renders identically in DataPraat — same hex values, same names."
    - "Tailwind utility classes like `bg-ink`, `text-ink-soft`, `border-line`, `bg-primary-tint` resolve to the prototype's hex values."
    - "shadcn CLI is configured (`components.json` exists with `style: \"base-vega\"`) and `pnpm dlx shadcn@latest add <name>` works."
    - "Inter / Fraunces / JetBrains Mono are self-hosted via next/font and exposed as `--font-sans` / `--font-display` / `--font-mono` CSS vars."
    - "fmtEUR / fmtNum / fmtPercent / fmtCompact are importable from `@/lib/format` and pass the Wave-0 RED tests."
  artifacts:
    - path: src/app/globals.css
      provides: "3-layer CSS: prototype :root tokens + shadcn aliases + @theme inline + scoped trust/ask-btn classes"
      contains: "--ink-soft: #5E5A53"
    - path: components.json
      provides: "shadcn config (style: base-vega, css: src/app/globals.css)"
      contains: "base-vega"
    - path: src/lib/utils.ts
      provides: "shadcn cn() helper (auto-written by shadcn init)"
      contains: "twMerge"
    - path: src/app/layout.tsx
      provides: "next/font wiring for Inter + Fraunces (axes:opsz) + JetBrains Mono → CSS vars"
      contains: "next/font/google"
    - path: src/lib/format/index.ts
      provides: "4 Dutch-locale format helpers as tree-shakable named exports"
      contains: "Intl.NumberFormat(\"nl-NL\""
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css' + className with CSS vars"
      pattern: "inter\\.variable"
    - from: "src/app/globals.css"
      to: "next/font CSS vars"
      via: "@theme inline { --font-sans: var(--font-sans) }"
      pattern: "--font-sans"
    - from: "src/lib/format/index.ts"
      to: "src/lib/format/index.test.ts"
      via: "import { fmtEUR, fmtNum, fmtPercent, fmtCompact } from '.'"
      pattern: "Intl\\.NumberFormat"
---

<objective>
Wave 1 ports the prototype's visual language into the Next.js app. After this plan ships:
1. `globals.css` is the verbatim 3-layer skeleton from RESEARCH.md §3 (prototype `:root` + shadcn aliases incl. 8 sidebar tokens + `@theme inline` + scoped `.trust.*` / `.ask-btn-*` classes).
2. shadcn CLI is initialised against `base-vega` with `components.json` committed.
3. `next/font/google` self-hosts Inter / Fraunces (with `axes: ["opsz"]`) / JetBrains Mono and exposes CSS variables.
4. The 4 Dutch-locale format helpers exist at `src/lib/format/index.ts`, turning Wave-0 format tests GREEN.
5. `@tabler/icons-react` is installed (D-06 fallback well; not consumed in Phase 2).

This is the foundation Wave 2 (custom primitives) and Wave 3 (shadcn primitives + /internal/design page) build on.

Purpose: Make every later phase consume tokens and primitives instead of re-inventing styling. FOUND-02, FOUND-03, DS-01 are all satisfied here. DS-03 is partially satisfied (the 4 format helpers; Wave 2 ships the 3 React primitives).

Output: 7 files modified/created — `package.json` (5 new deps), `components.json` (new), `src/lib/utils.ts` (new — shadcn writes it), `src/app/globals.css` (replaced verbatim with §3 skeleton), `src/app/layout.tsx` (extended with next/font), `src/lib/format/index.ts` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system/02-CONTEXT.md
@.planning/phases/02-design-system/02-RESEARCH.md
@.planning/phases/02-design-system/02-VALIDATION.md
@.planning/phases/02-design-system/02-01-PLAN.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@styles.css
@shared.jsx
@src/app/layout.tsx
@src/app/globals.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run shadcn init (base-vega) + install @tabler/icons-react</name>
  <files>package.json, pnpm-lock.yaml, components.json, src/lib/utils.ts</files>
  <read_first>
    - .planning/phases/02-design-system/02-CONTEXT.md D-04, D-06 (locked init flow + Tabler-as-fallback-well)
    - .planning/phases/02-design-system/02-RESEARCH.md §1 "Standard Stack" (versions) + §2 (install commands) + §6 "components.json after shadcn init" (expected file shape) + §"Pitfall 1" (shadcn init overwrites globals.css — handled in Task 2 next, NOT this task)
    - package.json (current state — verify Phase 1 exact-pin policy stays intact)
  </read_first>
  <action>
    **Step 1 — shadcn init.** Run the canonical Phase 2 init (D-04):

    ```bash
    pnpm dlx shadcn@latest init --yes --base base --style vega
    ```

    Expected outcome:
    - `components.json` written at repo root with `"style": "base-vega"`, `"tailwind.config": ""` (v4 mode), `"tailwind.css": "src/app/globals.css"`, `"cssVariables": true`, `"baseColor": "neutral"`, `"iconLibrary": "lucide"`, RSC + TSX both true, aliases pointing at `@/components`, `@/lib/utils`, `@/components/ui`, `@/lib`, `@/hooks`. Match RESEARCH.md §"components.json after shadcn init" — if init writes a different shape, edit to match.
    - `src/lib/utils.ts` written by the CLI with `cn()` helper (uses `clsx` + `tailwind-merge`).
    - `clsx` + `tailwind-merge` added to dependencies (CLI auto-installs).
    - `src/app/globals.css` REWRITTEN by the CLI with shadcn's default `:root` block (this is RESEARCH.md Pitfall 1 — DO NOT commit yet, Task 2 will replace this file with the §3 skeleton).
    - `lucide-react` may be added as a dependency by the CLI — leave it (D-06 says we don't import it; it's a transitive shadcn-snippet dep).

    **Step 2 — install @tabler/icons-react** at the exact pin (RESEARCH.md §1 row 2):

    ```bash
    pnpm add @tabler/icons-react@3.41.1
    ```

    **DO NOT** run `pnpm dlx shadcn@latest add ...` for any primitive in this task — that's Plan 04. If the init output prompts to add primitives interactively, decline.

    **DO NOT** modify `src/app/globals.css` in this task — Task 2 replaces it wholesale with the §3 skeleton. Leave the shadcn-init-generated default in place; Task 2 will overwrite it before commit.

    **Pin discipline:** if `pnpm install` adds caret/tilde ranges for `clsx`, `tailwind-merge`, `class-variance-authority`, `lucide-react`, etc., re-pin them to exact versions per RESEARCH.md §1 table after init completes:
    - `clsx@2.1.1`
    - `tailwind-merge@3.5.0`
    - `class-variance-authority@0.7.1`
    - (lucide-react — accept whatever shadcn pulls; we don't use it)

    Verify clean tree:
    ```bash
    pnpm exec tsc --noEmit && pnpm exec eslint .
    ```

    Commit: `feat(02-02): init shadcn base-vega + add @tabler/icons-react`
  </action>
  <verify>
    <automated>test -f components.json && test -f src/lib/utils.ts && grep -q '"style": "base-vega"' components.json && grep -q '"tailwind.config": ""' components.json && grep -q '"css": "src/app/globals.css"' components.json && grep -q 'twMerge\|tailwind-merge' src/lib/utils.ts && node -e "const p=require('./package.json'); const all={...p.dependencies,...p.devDependencies}; if(all['@tabler/icons-react']!=='3.41.1'){console.error('tabler pin wrong:',all['@tabler/icons-react']);process.exit(1);} if(!all.clsx||!all['tailwind-merge']){console.error('shadcn helpers missing');process.exit(1);} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists at repo root with literal strings: `"style": "base-vega"`, `"tailwind.config": ""`, `"css": "src/app/globals.css"`, `"cssVariables": true`, `"rsc": true`, `"tsx": true`.
    - `src/lib/utils.ts` exists, exports `cn` function, references `tailwind-merge` (via `twMerge` import or string).
    - `package.json` deps contain EXACTLY `"@tabler/icons-react": "3.41.1"` (no caret/tilde).
    - `package.json` contains `clsx`, `tailwind-merge`, `class-variance-authority` (versions match RESEARCH.md §1 table; tolerate caret/tilde here only if pin-rewrite caused conflicts — ideally exact).
    - `package.json` does NOT contain any of: `@radix-ui/*`, `@base-ui/*`, `sonner`, individual primitive packages — those land in Plan 04 when we run `shadcn add`.
    - `pnpm exec tsc --noEmit` exits 0.
    - `pnpm exec eslint .` exits 0.
    - `pnpm-lock.yaml` updated and committed.
  </acceptance_criteria>
  <done>shadcn CLI initialised against base-vega; components.json + src/lib/utils.ts committed; @tabler/icons-react pinned; no shadcn primitives pre-installed.</done>
</task>

<task type="auto">
  <name>Task 2: Replace globals.css with the verbatim §3 hybrid-token skeleton (incl. sidebar aliases)</name>
  <files>src/app/globals.css</files>
  <read_first>
    - .planning/phases/02-design-system/02-RESEARCH.md §3 "Pattern 1: Hybrid token cascade" (THE source-of-truth skeleton, ~270 lines — copy verbatim)
    - .planning/phases/02-design-system/02-RESEARCH.md §"Open Questions (RESOLVED)" Q1 (the sidebar-alias addition — RESEARCH-flagged correction to CONTEXT.md D-01)
    - .planning/phases/02-design-system/02-RESEARCH.md §"Pitfall 1" + §"Pitfall 2" + §"Pitfall 6" (why we replace shadcn's default; OKLCH-to-hex; @theme namespace requirement)
    - styles.css lines 1-100 (prototype :root — verify hex values match what we're about to write)
    - .planning/phases/02-design-system/02-CONTEXT.md D-01, D-02, D-03 (no renames, hex not OKLCH, hybrid token model)
    - src/app/globals.css (current state — what shadcn init wrote in Task 1; we wholesale replace)
  </read_first>
  <action>
    Replace `src/app/globals.css` ENTIRELY with the RESEARCH.md §3 skeleton verbatim. The skeleton has 4 layers in this exact order:

    1. `@import "tailwindcss";`
    2. **Layer 1** — `:root { ... prototype tokens ... }`. Port verbatim from `styles.css` lines 3-79: `--bg`, `--bg-soft`, `--surface`, `--surface-raised`, `--ink`, `--ink-soft`, `--ink-mute`, `--ink-faint`, `--line`, `--line-soft`, `--line-strong`, `--primary`, `--primary-soft`, `--primary-tint`, `--primary-ink`, `--primary-foreground`, `--chart-cost`, `--chart-volume`, `--chart-price`, `--chart-1` through `--chart-5`, `--gold`, `--gold-tint`, `--success`, `--success-tint`, `--success-strong`, `--warning`, `--warning-tint`, `--warning-strong`, `--destructive`, `--destructive-tint`, `--destructive-strong`, `--r-sm/md/lg/xl`, `--s-xs/sm/md/lg/xl/2xl`, `--shadow-sm/md/lg/drawer`, plus font-stack aliases `--f-sans: var(--font-sans)`, `--f-serif: var(--font-display)`, `--f-mono: var(--font-mono)`.
       - **Hex values** are verbatim from `styles.css` (D-02 — no OKLCH). Critical pins to verify against grep: `--ink-soft: #5E5A53`, `--primary: #4338CA`, `--chart-cost: #3b82f6`, `--success: #5E8A6D`.
       - The `--f-sans/--f-serif/--f-mono` aliases reference `--font-sans/--font-display/--font-mono` which Task 3 sets up via `next/font` in `layout.tsx`. Don't define them as static font-stack strings here — they MUST chain through next/font's CSS vars so font self-hosting works.
    3. **Layer 2** — `:root { ... shadcn aliases ... }`. shadcn-expected variable names mapped to prototype tokens:
       - Core: `--background: var(--bg)`, `--foreground: var(--ink)`, `--card: var(--surface)`, `--card-foreground: var(--ink)`, `--popover: var(--surface)`, `--popover-foreground: var(--ink)`, `--primary: var(--primary)` (same name OK), `--primary-foreground: var(--primary-foreground)` (same name OK), `--secondary: var(--bg-soft)`, `--secondary-foreground: var(--ink)`, `--muted: var(--bg-soft)`, `--muted-foreground: var(--ink-mute)`, `--accent: var(--primary-tint)`, `--accent-foreground: var(--primary-ink)`, `--destructive: var(--destructive)`, `--border: var(--line)`, `--input: var(--line-strong)`, `--ring: var(--primary-soft)`, `--radius: var(--r-md)`.
       - **Sidebar (CRITICAL — RESEARCH.md correction to CONTEXT.md D-01):** add 8 sidebar aliases so a future `shadcn add sidebar` doesn't fail theme resolution:
         - `--sidebar: var(--bg-soft)`
         - `--sidebar-foreground: var(--ink)`
         - `--sidebar-primary: var(--primary)`
         - `--sidebar-primary-foreground: var(--primary-foreground)`
         - `--sidebar-accent: var(--primary-tint)`
         - `--sidebar-accent-foreground: var(--primary-ink)`
         - `--sidebar-border: var(--line)`
         - `--sidebar-ring: var(--primary-soft)`
    4. **Layer 3** — `@theme inline { ... }` mapping prototype tokens to Tailwind utility namespaces. ALL of:
       - `--color-bg`, `--color-bg-soft`, `--color-surface`, `--color-surface-raised`
       - `--color-ink`, `--color-ink-soft`, `--color-ink-mute`, `--color-ink-faint`
       - `--color-line`, `--color-line-soft`, `--color-line-strong`
       - `--color-primary`, `--color-primary-soft`, `--color-primary-tint`, `--color-primary-ink`, `--color-primary-foreground`
       - `--color-chart-cost`, `--color-chart-volume`, `--color-chart-price`, `--color-chart-1..5`
       - `--color-gold`, `--color-gold-tint`
       - `--color-success`, `--color-success-tint`, `--color-success-strong`, `--color-warning`, `--color-warning-tint`, `--color-warning-strong`, `--color-destructive`, `--color-destructive-tint`, `--color-destructive-strong`
       - shadcn semantic colors: `--color-background`, `--color-foreground`, `--color-card`, `--color-card-foreground`, `--color-popover`, `--color-popover-foreground`, `--color-secondary`, `--color-secondary-foreground`, `--color-muted`, `--color-muted-foreground`, `--color-accent`, `--color-accent-foreground`, `--color-border`, `--color-input`, `--color-ring`
       - `--color-sidebar`, `--color-sidebar-foreground`, `--color-sidebar-primary`, `--color-sidebar-primary-foreground`, `--color-sidebar-accent`, `--color-sidebar-accent-foreground`, `--color-sidebar-border`, `--color-sidebar-ring`
       - Radius: `--radius-sm: var(--r-sm)`, `--radius-md: var(--r-md)`, `--radius-lg: var(--r-lg)`, `--radius-xl: var(--r-xl)`
       - Spacing: `--spacing-xs/sm/md/lg/xl/2xl` (each `var(--s-XX)`)
       - Fonts: `--font-sans: var(--font-sans)`, `--font-display: var(--font-display)`, `--font-mono: var(--font-mono)` (chains through to next/font)
       - Shadows: `--shadow-sm/md/lg/drawer` (each `var(--shadow-XX)`)
    5. **Layer 4** — Scoped CSS classes ported from prototype that Wave 2 primitives need:
       - `.trust { ... }`, `.trust-dot { ... }`, `.trust.good`, `.trust.warn`, `.trust.bad` + their dot color variants — verbatim from RESEARCH.md §3.
       - `.ask-btn { ... }` + `.ask-btn:hover` + `.ask-btn .orb` + `.ask-btn-sm/md/lg` + size-scoped `.orb` variants — verbatim from RESEARCH.md §3.
       - `body { ... }` baseline (font-family, color, background, font-feature-settings, font-size, line-height, antialiasing) — replaces the scaffold's `font-family: Arial`.

    **Order discipline (RESEARCH.md §3 critical):**
    - `@import "tailwindcss"` MUST be first.
    - Layer 1 BEFORE Layer 2 so the aliases can `var(--bg)` etc.
    - `@theme inline` AFTER both `:root` blocks — `inline` resolves `var()` at generation time so `bg-ink-soft` utility actually generates with the hex value.
    - Layer 4 scoped classes AFTER `@theme` so utilities don't outrank them in specificity battles.

    **Do NOT include:**
    - The prefers-color-scheme dark block from the scaffold's globals.css (D-14 defers dark mode).
    - Any OKLCH values — pure hex per D-02.
    - `@theme` (without `inline`) — must be `@theme inline` per RESEARCH.md Pitfall 2.
    - Page-level scoped classes (`.kpi-*`, `.sb-*`, `.card`, `.page-title`, etc.) — those land in their consuming phase per D-15 / consuming phase ownership.

    Commit: `feat(02-02): port prototype tokens to globals.css with hybrid 3-layer cascade`
  </action>
  <verify>
    <automated>grep -q '@import "tailwindcss"' src/app/globals.css && grep -q '\-\-ink-soft: #5E5A53' src/app/globals.css && grep -q '\-\-primary: #4338CA' src/app/globals.css && grep -q '\-\-chart-cost: #3b82f6' src/app/globals.css && grep -q '\-\-success: #5E8A6D' src/app/globals.css && grep -q '\-\-background: var(--bg)' src/app/globals.css && grep -q '\-\-sidebar: var(--bg-soft)' src/app/globals.css && grep -q '\-\-sidebar-ring: var(--primary-soft)' src/app/globals.css && grep -q '@theme inline' src/app/globals.css && grep -q '\-\-color-ink-soft: var(--ink-soft)' src/app/globals.css && grep -q '\-\-radius-md: var(--r-md)' src/app/globals.css && grep -q '\-\-spacing-lg: var(--s-lg)' src/app/globals.css && grep -q '.trust.good' src/app/globals.css && grep -q '.ask-btn-sm' src/app/globals.css && grep -q '.ask-btn .orb' src/app/globals.css && ! grep -q 'oklch' src/app/globals.css && ! grep -q 'prefers-color-scheme' src/app/globals.css && pnpm build > /tmp/build.log 2>&1 && echo "build-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` first non-blank line is `@import "tailwindcss";`.
    - Layer 1 (`:root`) contains the 5 critical hex pins: `--ink-soft: #5E5A53`, `--primary: #4338CA`, `--chart-cost: #3b82f6`, `--success: #5E8A6D`, `--destructive: #A85050`.
    - Layer 1 references font-stack aliases: `--f-sans: var(--font-sans)`, `--f-serif: var(--font-display)`, `--f-mono: var(--font-mono)`.
    - Layer 2 contains the 8 sidebar aliases (greppable: `--sidebar: var(--bg-soft)` and `--sidebar-ring: var(--primary-soft)`).
    - Layer 2 contains core shadcn aliases: `--background: var(--bg)`, `--foreground: var(--ink)`, `--card: var(--surface)`, `--ring: var(--primary-soft)`, `--radius: var(--r-md)`.
    - Layer 3 (`@theme inline`) — file contains literal `@theme inline` (with the `inline` modifier per Pitfall 2).
    - Layer 3 contains representative utility-mapping tokens: `--color-ink-soft: var(--ink-soft)`, `--color-primary-tint: var(--primary-tint)`, `--color-chart-cost: var(--chart-cost)`, `--radius-md: var(--r-md)`, `--spacing-lg: var(--s-lg)`, `--color-sidebar: var(--sidebar)`.
    - Layer 4 scoped classes: `.trust.good`, `.trust.warn`, `.trust.bad`, `.trust-dot`, `.ask-btn`, `.ask-btn-sm`, `.ask-btn-md`, `.ask-btn-lg`, `.ask-btn .orb` all present.
    - File contains NO `oklch(` values.
    - File contains NO `prefers-color-scheme` block (dark mode deferred per D-14).
    - File contains NO page-level styles (`.kpi-*`, `.sb-*`, `.card-*`, `.page-title`) — those land in consuming phases.
    - `pnpm build` exits 0 — Tailwind generates utilities from `@theme inline` without errors.
    - `pnpm exec prettier --check src/app/globals.css` exits 0.
  </acceptance_criteria>
  <done>globals.css is the verbatim 3-layer hybrid skeleton; prototype tokens preserved at hex values; shadcn aliases (incl. 8 sidebar) wired; @theme inline generates Tailwind utilities; .trust/.ask-btn classes present for Wave 2 primitives.</done>
</task>

<task type="auto">
  <name>Task 3: Wire next/font (Inter + Fraunces opsz + JetBrains Mono) in layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - .planning/phases/02-design-system/02-RESEARCH.md §"Code Examples" "next/font setup in layout.tsx" (the verbatim layout.tsx snippet — copy)
    - .planning/phases/02-design-system/02-RESEARCH.md §"Pitfall 3" (Fraunces opsz axis caveat — declare but consumers must set font-variation-settings)
    - .planning/phases/02-design-system/02-CONTEXT.md D-13 (locked next/font decision)
    - src/app/layout.tsx (current Phase 1 state — extends, doesn't rewrite)
  </read_first>
  <action>
    Replace `src/app/layout.tsx` with the RESEARCH.md §"Code Examples" verbatim layout. The shape is:

    ```tsx
    // src/app/layout.tsx
    import type { Metadata } from "next";
    import { Inter, Fraunces, JetBrains_Mono } from "next/font/google";
    import "./globals.css";

    const inter = Inter({
      subsets: ["latin", "latin-ext"],
      weight: ["300", "400", "500", "600", "700"],
      variable: "--font-sans",
      display: "swap",
    });

    const fraunces = Fraunces({
      subsets: ["latin", "latin-ext"],
      weight: ["300", "400", "500", "600"],
      axes: ["opsz"],
      variable: "--font-display",
      display: "swap",
    });

    const jetbrains = JetBrains_Mono({
      subsets: ["latin", "latin-ext"],
      weight: ["400", "500"],
      variable: "--font-mono",
      display: "swap",
    });

    export const metadata: Metadata = {
      title: "DataPraat",
      description: "Praat met je gemeentedata in eenvoudig Nederlands.",
    };

    export default function RootLayout({
      children,
    }: Readonly<{ children: React.ReactNode }>) {
      return (
        <html
          lang="nl"
          className={`${inter.variable} ${fraunces.variable} ${jetbrains.variable}`}
        >
          <body>{children}</body>
        </html>
      );
    }
    ```

    **Critical details (per D-13 + Pitfall 3):**
    - Subsets `["latin", "latin-ext"]` for both fonts — Dutch diacritics live in `latin-ext` (ë in Jeugdbescherming etc.).
    - Fraunces `axes: ["opsz"]` registers the optical-size variable axis. Per RESEARCH.md Pitfall 3: `font-variation-settings` is NOT auto-applied; consumers in Phase 6 (verhaal mode) set it explicitly. Add inline comment: `// Fraunces opsz axis exposed via variable; consumers set font-variation-settings explicitly per Phase 6 verhaal mode.`
    - All three fonts use `display: "swap"` (matches Next.js docs default for self-hosted Google fonts; avoids FOIT).
    - The 3 CSS variables (`--font-sans`, `--font-display`, `--font-mono`) chain into globals.css `@theme inline` — Layer 3 maps them through to Tailwind's `font-{sans,display,mono}` utilities.
    - Preserve `<html lang="nl">` (Phase 1 contract; matches CLAUDE.md "Dutch UI").
    - Preserve `metadata` (already correct per Phase 1).
    - DO NOT add `data-theme="..."` attribute — dark mode deferred per D-14.

    **DO NOT** create `src/app/fonts.ts` or any separate fonts module — RESEARCH.md "Anti-Patterns" + Next.js docs both say declare once in `layout.tsx`. If a later phase needs the font instances elsewhere, refactor at that point.

    Commit: `feat(02-02): self-host Inter + Fraunces (opsz) + JetBrains Mono via next/font`
  </action>
  <verify>
    <automated>grep -q 'next/font/google' src/app/layout.tsx && grep -q 'Inter\b' src/app/layout.tsx && grep -q 'Fraunces' src/app/layout.tsx && grep -q 'JetBrains_Mono' src/app/layout.tsx && grep -q 'axes: \["opsz"\]' src/app/layout.tsx && grep -q 'variable: "--font-sans"' src/app/layout.tsx && grep -q 'variable: "--font-display"' src/app/layout.tsx && grep -q 'variable: "--font-mono"' src/app/layout.tsx && grep -qE 'subsets:\s*\["latin",\s*"latin-ext"\]' src/app/layout.tsx && grep -q 'lang="nl"' src/app/layout.tsx && grep -qE 'inter\.variable.*fraunces\.variable.*jetbrains\.variable|inter\.variable\}\s*\$\{fraunces' src/app/layout.tsx && pnpm build > /tmp/build-fonts.log 2>&1 && grep -qE 'Compiled successfully|✓ Compiled' /tmp/build-fonts.log && echo "build-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` imports `Inter`, `Fraunces`, `JetBrains_Mono` from `next/font/google`.
    - Each font instance sets `variable: "--font-sans" | "--font-display" | "--font-mono"` respectively.
    - All three include `subsets: ["latin", "latin-ext"]`.
    - Fraunces declaration includes `axes: ["opsz"]`.
    - Inter weights include 300, 400, 500, 600, 700.
    - Fraunces weights include 300, 400, 500, 600.
    - JetBrains_Mono weights include 400, 500.
    - `<html>` element has `lang="nl"` and `className` combining all 3 font `.variable` strings.
    - `<body>` does NOT have hardcoded `font-family` (the rule is in globals.css `body` selector).
    - `pnpm build` exits 0 — fonts download at build time, no Google Fonts errors.
    - `pnpm exec tsc --noEmit` exits 0.
    - `pnpm exec eslint src/app/layout.tsx` exits 0.
    - File contains a brief comment near Fraunces about `font-variation-settings` being consumer-set (per Pitfall 3).
  </acceptance_criteria>
  <done>3 fonts self-hosted via next/font with CSS-var hand-off; Dutch latin-ext subset covered; Fraunces opsz axis exposed for Phase 6; build successful.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement src/lib/format/index.ts — turns Wave-0 format tests GREEN</name>
  <files>src/lib/format/index.ts</files>
  <behavior>
    The 12 RED tests from `src/lib/format/index.test.ts` (Plan 01 Wave-0) define the exact contract. After this task, `pnpm exec vitest --run src/lib/format/index.test.ts` exits 0 with 12 GREEN.

    Behaviors:
    - `fmtEUR(7_200_000)` → matches `/€\s*7\.200\.000/` (nl-NL Intl with currency:EUR + maximumFractionDigits:0)
    - `fmtNum(28_400)` → `"28.400"` (nl-NL Intl default, dot thousands)
    - `fmtPercent(0.85)` → matches `/85.*%/` (style:percent, maximumFractionDigits:1)
    - `fmtCompact(7_200_000)` → `"€7,2M"` (custom formatting: divide by 1M, toLocaleString nl-NL with maxFractionDigits:1, prepend €, append M)
    - `fmtCompact(450_000)` → `"€450K"` (divide by 1K, Math.round, prepend €, append K)
    - `fmtCompact(450)` → falls back to `fmtEUR(450)`
  </behavior>
  <read_first>
    - shared.jsx lines 62-68 (verbatim port source for fmtEUR / fmtNum / fmtCompact)
    - .planning/phases/02-design-system/02-RESEARCH.md §"Code Examples" "src/lib/format/index.ts" (the verbatim TS port + new fmtPercent)
    - .planning/phases/02-design-system/02-CONTEXT.md D-11, D-12 (locked port + tree-shakable named exports + nl-NL hard-coded)
    - src/lib/format/index.test.ts (Wave-0 — the 12 RED tests this turns GREEN)
  </read_first>
  <action>
    Create `src/lib/format/index.ts` verbatim from RESEARCH.md §"Code Examples":

    ```ts
    // src/lib/format/index.ts — Dutch-locale number/currency/percent helpers.
    // All Intl.NumberFormat instances are constructed per call rather than memoised:
    // V8 caches instances internally, and Phase 2 callsites are rare enough that
    // the cleaner shape wins over the optimisation.

    export function fmtEUR(n: number): string {
      return new Intl.NumberFormat("nl-NL", {
        style: "currency",
        currency: "EUR",
        maximumFractionDigits: 0,
      }).format(n);
    }

    export function fmtNum(n: number): string {
      return new Intl.NumberFormat("nl-NL").format(n);
    }

    export function fmtPercent(n: number): string {
      return new Intl.NumberFormat("nl-NL", {
        style: "percent",
        maximumFractionDigits: 1,
      }).format(n);
    }

    export function fmtCompact(n: number): string {
      if (n >= 1_000_000) {
        return `€${(n / 1_000_000).toLocaleString("nl-NL", { maximumFractionDigits: 1 })}M`;
      }
      if (n >= 1_000) {
        return `€${Math.round(n / 1_000)}K`;
      }
      return fmtEUR(n);
    }
    ```

    **Implementation notes:**
    - Tree-shakable named exports only — NO default export, NO barrel object (D-12).
    - Locale hard-coded `"nl-NL"` — multi-locale is `<deferred>` (D-11, RESEARCH §"Deferred Ideas").
    - `fmtPercent` is the only NEW helper (not in prototype) — added per ROADMAP success criterion #3 + D-11.
    - Each helper is `O(1)` and side-effect-free; no module-level state.
    - No `try/catch` — `Intl.NumberFormat` doesn't throw on numeric input; nullish/non-number inputs are caller responsibility (TS strict catches at compile time).

    Run the Wave-0 tests to confirm GREEN:
    ```bash
    pnpm exec vitest --run src/lib/format/index.test.ts
    ```
    Expect: 12 tests passed.

    If any test fails, the failure is either (a) a contract mismatch in the test (Plan 01 bug — fix the test, not the implementation) OR (b) Intl variance across Node versions (use regex matchers in tests, not literals). Do NOT add platform-specific branches to the implementation.

    Commit: `feat(02-02): add Dutch-locale format helpers (fmtEUR, fmtNum, fmtPercent, fmtCompact)`
  </action>
  <verify>
    <automated>test -f src/lib/format/index.ts && grep -q 'export function fmtEUR' src/lib/format/index.ts && grep -q 'export function fmtNum' src/lib/format/index.ts && grep -q 'export function fmtPercent' src/lib/format/index.ts && grep -q 'export function fmtCompact' src/lib/format/index.ts && grep -q '"nl-NL"' src/lib/format/index.ts && grep -q 'currency: "EUR"' src/lib/format/index.ts && grep -q 'style: "percent"' src/lib/format/index.ts && ! grep -q 'export default' src/lib/format/index.ts && pnpm exec vitest --run src/lib/format/index.test.ts 2>&1 | grep -E '(12 passed|Tests\s+12 passed)' && echo "format-tests-green"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/format/index.ts` exists with 4 named function exports: `fmtEUR`, `fmtNum`, `fmtPercent`, `fmtCompact`.
    - File contains literal strings: `"nl-NL"`, `currency: "EUR"`, `style: "percent"`, `maximumFractionDigits: 1` (for fmtPercent), `maximumFractionDigits: 0` (for fmtEUR).
    - File contains NO default export (`! grep -q 'export default' src/lib/format/index.ts`).
    - File contains NO `try/catch` blocks.
    - File contains NO module-level state (no top-level `const fmt = new Intl.NumberFormat(...)` cached instances) — D-12 + RESEARCH §"Code Examples" comment about per-call construction.
    - `pnpm exec vitest --run src/lib/format/index.test.ts` exits 0 with 12 tests passing.
    - `pnpm exec tsc --noEmit` exits 0.
    - `pnpm exec eslint src/lib/format/index.ts` exits 0.
    - `pnpm build` exits 0 — confirms tree-shaking works (no production-bundle bloat).
  </acceptance_criteria>
  <done>4 format helpers exported as tree-shakable named exports; nl-NL hard-coded; 12 Wave-0 tests turn GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → npm registry | shadcn CLI fetches templates from ui.shadcn.com; pnpm fetches packages from npm. Untrusted code surface during install. |
| build → Google Fonts | next/font downloads font files from fonts.googleapis.com at build time and self-hosts them in `.next/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02 | Tampering | Supply chain (shadcn CLI + auto-pulled deps `clsx`, `tailwind-merge`, `class-variance-authority`, `lucide-react`, `@tabler/icons-react`) | mitigate | Pin every direct dep at exact version; commit `pnpm-lock.yaml` after each install (Phase 1 exact-pin policy continues); review the dep list in plan SUMMARY. |
| T-2-03 | Tampering | next/font fetch from Google Fonts at build time | accept | This is a documented Next.js risk, not Phase-2-specific. Pinning Next.js to 15.5.15 (Phase 1) is the upstream mitigation. Self-hosted output lives in `.next/static/` after build; runtime is independent of Google Fonts. No Phase-2 work. |
| T-2-04 | Spoofing | Prototype CSS class names (`.ask-btn-*`, `.trust.*`) ported to globals.css | accept | Static CSS only; no template/string interpolation; no user input touches these class names. Low risk. |

No `high` severity threats. Two `low` accepts (T-2-03 documented Next.js model; T-2-04 static CSS). One `low` mitigate (T-2-02 — exact pinning).
</threat_model>

<verification>
1. `pnpm exec tsc --noEmit` exits 0.
2. `pnpm exec eslint .` exits 0.
3. `pnpm exec prettier --check .` exits 0.
4. Phase 2 Wave-0 format tests now GREEN (component tests still RED — Plan 03 turns them GREEN):
   - `pnpm exec vitest --run src/lib/format/index.test.ts` → 12 passed.
   - `pnpm exec vitest --run src/components/design` → still RED (component files don't exist yet — expected).
5. Phase 1 GREEN tests still pass: `pnpm exec vitest --run scripts/check-env-example.test.ts` → 1 passed.
6. `pnpm build` exits 0 — Tailwind utilities generate from `@theme inline`, fonts self-host, no missing deps.
7. `bash scripts/check-standalone.sh` exits 0 — Phase 1 standalone build still intact.
8. Manual smoke (optional): `pnpm dev` + visit `localhost:3000`; the placeholder page now renders in Inter (not Arial), proving the font wiring works.
</verification>

<success_criteria>
- shadcn CLI initialised: `components.json` exists with `style: "base-vega"`; `src/lib/utils.ts` written with `cn()`.
- `@tabler/icons-react@3.41.1` installed (D-06 fallback well, not consumed in Phase 2).
- `globals.css` is the verbatim §3 hybrid skeleton: prototype `:root` + shadcn aliases (incl. 8 sidebar) + `@theme inline` + `.trust.*` + `.ask-btn-*` scoped classes.
- All 5 critical hex pins present (`--ink-soft: #5E5A53`, `--primary: #4338CA`, `--chart-cost: #3b82f6`, `--success: #5E8A6D`, `--destructive: #A85050`).
- No `oklch()` in globals.css (D-02).
- No dark-mode `prefers-color-scheme` block (D-14 deferred).
- `layout.tsx` self-hosts Inter / Fraunces (axes:opsz) / JetBrains Mono via `next/font/google` with CSS variables `--font-sans` / `--font-display` / `--font-mono`.
- `latin-ext` subset present on all 3 fonts (Dutch diacritics).
- `src/lib/format/index.ts` exports 4 helpers (fmtEUR/fmtNum/fmtPercent/fmtCompact) as named exports — Wave-0 RED tests turn GREEN (12/12).
- Build exits 0; tsc/eslint/prettier exit 0; standalone build intact.
- No shadcn primitives pre-installed (those land in Plan 04).
- No AI SDK / MCP SDK / Recharts installed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-02-SUMMARY.md` per the standard SUMMARY template, including:
- Tabular list of every dep added (direct + auto-pulled by shadcn init) with the exact version pinned.
- Confirmation that the two CONTEXT.md corrections (toast→sonner planned for Plan 04; sidebar aliases shipped here) were applied — DO NOT modify CONTEXT.md.
- Snippet of the final globals.css order (4 layers) so the verifier can spot drift.
- Build evidence: paste the `pnpm build` summary lines (route count, bundle sizes).
- Note that Plan 03 (Wave 2) immediately consumes the `.trust.*` and `.ask-btn-*` classes from globals.css.
</output>
